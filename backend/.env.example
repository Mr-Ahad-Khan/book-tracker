# MySQL Workbench connection details
DB_HOST=localhost
DB_PORT=3306
DB_USER=book_tracker_user
DB_PASSWORD=root
DB_NAME=book_tracker
# Set true only when your hosted MySQL provider requires TLS.
DB_SSL=false

# The hosting provider supplies this in production.
PORT=3000
